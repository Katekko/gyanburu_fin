name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      VERSION: '3.4.4'
      SERVERPOD_PASSWORD_database: '9wkXNWDTld3wiKR4C7E0Bi6KOW5T80UE'
      SERVERPOD_PASSWORD_redis: 'mqVgNO8FpYlP3mP7YziIVrJuVzNgtxMd'

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.8'

      - name: Start containers
        working-directory: gyanburu_fin_server
        run: docker compose up --quiet-pull --quiet-build --build -d

      - name: Install dependencies
        working-directory: gyanburu_fin_server
        run: dart pub get

      - name: Install Serverpod CLI
        working-directory: gyanburu_fin_server
        run: dart pub global activate serverpod_cli $VERSION

      - name: Run Serverpod generator
        working-directory: gyanburu_fin_server
        run: serverpod generate

      - name: Run tests
        working-directory: gyanburu_fin_server
        run: dart test

      - name: Stop containers
        if: always()
        working-directory: gyanburu_fin_server
        run: docker compose down -v
